# Markdown Spoilers
This is a neovim (nvim) plugin that hides text between double vertical pipes (`||`) behind a spoiler.

This mirrors the functionality in Discord's flavor of markdown.

`<C-CR>` reveals a spoiler.
`:MarkdownSpoilers close`
`:MarkdownSpoilers open`
